from django_filters import rest_framework as filters


class UserFilter(filters.FilterSet):
    """ A filter class for filtering user data.
    This class extends the `FilterSet` class from the `django_filters`
    package and provides filtering options for user data.

    Example:
        To use the `UserFilter` class, you can define it in your view or serializer class:
    """
    pass
